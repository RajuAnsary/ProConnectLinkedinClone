/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost", "proconnectlinkedincloneapnacollege.onrender.com", "images.pexels.com"],
  },
};

export default nextConfig;